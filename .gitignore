__pycache__/
*.pyc

# Debug output from `python3 adapters.py` / `python3 classifier.py`
postings.jsonl
needs_review.jsonl

# notify.py's alert files. It is gone; these stay listed so an old
# checkout that still has one does not commit it.
state/alert.md
state/alert_title.txt
